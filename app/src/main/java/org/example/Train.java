package org.example;

/**
 * Train
 */
public interface Train {

    int capacity = 6;
    int lower = (capacity / 3);
    int middle = (capacity / 3);
    int upper = (capacity / 3);
    int waiting = 2;
    int rac = 4;

   // void Book();
   // void Cancel();
   // void Available();
   // void Booked();
}

